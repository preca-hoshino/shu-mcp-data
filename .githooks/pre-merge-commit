#!/usr/bin/env bash

# 禁止在本地执行所有合并操作
echo -e "\033[0;31m❌ [Pre-Merge 拦截] 抱歉，禁止在本地执行合并操作！\033[0m"
echo "根据项目分支保护规范，所有的分支合并必须通过服务端的 Pull Request (PR) 或是 Merge Request (MR) 进行。"
echo "请将当前分支推送到远程仓库，并在网页端提交合入申请。"
echo "--------------------------------------------------------"
exit 1
